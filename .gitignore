# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local scratch / experiments (never committed)
.scratch/

# Internal, fund-specific analytical briefs (never publish/commit — compliance)
content/video/ideas/_analytical/

# Publishing secrets & rendered video (never commit)
publish/client_secret.json
publish/token.json
publish/*token*
publish/metadata.json
renders/
*.mp4

# Monitor run flag (ephemeral)
monitor/changed.flag

# local page-generation scratch
.build/

# options_desk: credentials and recorded market data (never commit either)
.env
.env.*
!.env.example
.options_data/
*.jsonl
*.jsonl.gz
