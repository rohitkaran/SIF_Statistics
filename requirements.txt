# Dependencies for the PORTFOLIO pipeline only (fetch_sif_portfolios.py).
# The NAV fetcher (fetch_amfi_sif.py) remains Python-stdlib-only and needs none of these.
openpyxl>=3.1      # read AMC monthly-portfolio .xlsx disclosures
pdfplumber>=0.11   # read AMC portfolio disclosures published as PDF
